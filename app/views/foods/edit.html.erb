<h1>Edit your Food</h1>
<%= form_for @food do |f| %>

	<%= label_tag(:item, "Item:")%>
	<%= f.text_field :item %></br>

	<%= label_tag(:description, "Description") %>
	<%= f.text_area :description %></br>

	<%= f.submit "Upadte" %>
	<% end %>